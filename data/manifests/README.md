# Data manifests

Every external dataset must have a versioned manifest containing source URL, retrieval date,
licence, SHA-256 checksum, preprocessing command, coordinate reference system, and units.
No external data is bundled in the initial synthetic release.
